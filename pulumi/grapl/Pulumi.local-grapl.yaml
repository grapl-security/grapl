encryptionsalt: v1:KRH5XXw/Vv4=:v1:Mi1EOttYFIuFy8Zl:u/QoTfZuh9c5m6Jyw2L3mAKC+VNs8g==
config:
  aws:accessKey: test
  aws:endpoints:
    - apigateway: http://localhost:4566
      cloudwatch: http://localhost:4566
      cloudwatchevents: http://localhost:4566
      cloudwatchlogs: http://localhost:4566
      dynamodb: http://localhost:4566
      ec2: http://localhost:4566
      iam: http://localhost:4566
      lambda: http://localhost:4566
      s3: http://localhost:4566
      secretsmanager: http://localhost:4566
      sns: http://localhost:4566
      sqs: http://localhost:4566
  aws:region: us-east-1
  aws:s3ForcePathStyle: "true"
  aws:secretKey: test
  aws:skipCredentialsValidation: "true"
  aws:skipRequestingAccountId: "true"
  grapl:MG_ALPHAS: dgraph.grapl.test:9080
  grapl:GRAPL_CDK_OPERATIONAL_ALARMS_EMAIL: "fake-ops-email@fake.domain"
  grapl:GRAPL_API_HOST: api.grapl.test
  kafka:bootstrapServers:
    - "kafka-broker:9092"
  kafka:tlsEnabled: false

  grapl:artifacts:
    # You probably don't want any service versions in here.

    # You DO need to specify AMIs (they're require_artifact()'d)
    # Since this is local-grapl, we don't need real AMIs for Localstack EC2
    grapl-nomad-consul-client:
      us-east-1: ami-000fake
    grapl-nomad-consul-server:
      us-east-1: ami-001fake
