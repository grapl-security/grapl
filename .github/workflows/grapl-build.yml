name: Grapl Build

on:
  check_run:
    types: [created, rerequested]

jobs:
  grapl-build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Build Grapl
        run: |
          docker-compose --build-args release_target=debug -f docker-compose.yml -f docker-compose.build.yml build
          docker system prune -f
