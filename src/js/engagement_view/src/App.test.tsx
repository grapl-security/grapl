import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

test('empty test', () => {
});

// test('renders learn react link', () => {
//   const { getByText } = render(<App />);
//   const graplElement = getByText("GRAPL");
//   expect(graplElement).toBeInTheDocument();
// });
