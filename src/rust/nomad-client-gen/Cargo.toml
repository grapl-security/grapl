[package]
name = "nomad-client-gen"
version = "1.0.0"
authors = ["OpenAPI Generator team and contributors"]
edition = "2018"

[dependencies]
serde = "^1.0"
serde_derive = "^1.0"
serde_json = "^1.0"
url = "^2.2"
grapl-workspace-hack = { version = "0.1", path = "../grapl-workspace-hack" }

[dependencies.reqwest]
version = "^0.11"
default_features = false
features = ["json", "multipart", "rustls-tls"]

[dev-dependencies]
