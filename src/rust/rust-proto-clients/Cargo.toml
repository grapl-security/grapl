[package]
name = "rust-proto-clients"
description = "A one-stop shop for getting clients to Grapl grpc services."
version = "0.0.1"
edition = "2021"

[dependencies]
clap = { version = "3.0", default_features = false, features = [
  "std",
  "env",
  "derive"
] }
rust-proto = { path = "../rust-proto" }
