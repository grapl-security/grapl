# How to install
Copy these files to $GRAPL_REPO_ROOT/.git/hooks - that's it!
Unfortunately, you cannot distribute the hooks pre-installed.
```
# replace as needed
cd $GRAPL_REPO_ROOT
cp etc/githooks/* .git/hooks
```
